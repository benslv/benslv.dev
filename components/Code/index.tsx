import { styled } from "stitches.config";

export const Code = styled("code", {
  color: "$accent",
  background: "$accent20",

  fontFamily: "$mono",
  fontWeight: "$semibold",

  padding: "2px 5px",
  borderRadius: "$1",

  lineHeight: 1.6,
});
