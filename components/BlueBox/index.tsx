import { css } from "../../stitches.config";

export const blueBox = css({
  color: "$accent",
  backgroundColor: "$accent20",

  padding: "calc(0.5 * $1) $1",

  borderRadius: "$1",

  boxShadow: "$light",
});
