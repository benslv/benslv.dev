import React from "react";
import { Layout } from "../components/Layout";

const Home = (): JSX.Element => {
  return (
    <Layout>
      <h1>Hello, world!</h1>
    </Layout>
  );
};

export default Home;
